암호화에서 말하는 **“키(Key)”**는 암호를 잠그고 여는 **비밀번호 같은 값**을 뜻합니다.   
하지만 단순한 비밀번호와는 달리, 키는 수학적으로 정의된 **숫자나 문자열**로서 암호화 알고리즘이 동작하는 핵심 재료

---

## 🔑 키(Key)의 의미
- **정의:** 암호화와 복호화를 수행할 때 사용하는 **비밀 값**  
- **역할:** 암호화 알고리즘은 공개된 규칙(수학적 공식)이지만, 키가 없으면 암호문을 원래 메시지로 되돌릴 수 없음  
- **비유:**  
  - 암호화 알고리즘 = 자물쇠의 구조  
  - 키 = 자물쇠를 여닫는 열쇠  

---

## ⚖️ 키의 종류
### 1. 대칭키 (Symmetric Key)
- **특징:** 암호화와 복호화에 **같은 키** 사용  
- **장점:** 빠르고 효율적  
- **단점:** 키를 상대방에게 안전하게 전달해야 하는 문제가 있음  
- **예시:** AES, DES  
- **비유:** 집 열쇠 하나를 가족 모두가 공유하는 상황 → 열쇠가 유출되면 누구나 들어올 수 있음  

### 2. 공개키 (Public Key) & 개인키 (Private Key)
- **특징:** 암호화와 복호화에 **서로 다른 키** 사용  
  - 공개키: 누구나 알 수 있음 (자물쇠를 잠그는 열쇠)  
  - 개인키: 본인만 알고 있음 (자물쇠를 여는 열쇠)  
- **장점:** 키 전달 문제 해결 (공개키는 공개해도 안전)  
- **단점:** 대칭키보다 계산량이 많아 느림  
- **예시:** RSA, ECC  
- **비유:** 우편함에 편지를 넣을 때는 누구나 열 수 있는 “투입구”가 있지만, 꺼낼 때는 집주인만 가진 “열쇠”가 필요함  

---

## 📊 비교 정리

| 구분 | 대칭키 | 공개키 |
|------|--------|--------|
| 키 사용 방식 | 암호화·복호화 동일 키 | 암호화·복호화 서로 다른 키 |
| 속도 | 빠름 | 상대적으로 느림 |
| 보안 | 키 전달이 문제 | 키 전달 문제 해결 |
| 대표 알고리즘 | AES, DES | RSA, ECC |

---

👉 정리하면, 암호화에서 **키는 암호를 잠그고 푸는 열쇠 역할을 하는 수학적 값**이고, 대칭키는 같은 열쇠를 공유하는 방식, 공개키는 서로 다른 열쇠를 쓰는 방식이라고 이해하면 됩니다.  

---  

#  **대칭키 암호화**와 **공개키 암호화**로 처리하는 과정을 아주 간단한 예시

## 1️⃣ 대칭키 암호화 (AES 예시)

대칭키는 암호화와 복호화에 **같은 키**를 사용합니다.

```python
from Crypto.Cipher import AES
import base64

# 메시지와 키 (16바이트 고정)
message = "Hello Symmetric World!"
key = b"thisisasecretkey"  # 대칭키

# AES 암호화 (ECB 모드)
cipher = AES.new(key, AES.MODE_ECB)
# AES는 블록 단위(16바이트)라 패딩 필요
pad = lambda s: s + (16 - len(s) % 16) * chr(16 - len(s) % 16)
encrypted = cipher.encrypt(pad(message).encode())

print("암호문:", base64.b64encode(encrypted).decode())

# 복호화
decrypted = cipher.decrypt(encrypted).decode()
print("복호문:", decrypted)
```

**출력 예시:**
```
암호문: 9kzX...==   (난수처럼 보이는 문자열)
복호문: Hello Symmetric World!
```

---

## 2️⃣ 공개키 암호화 (RSA 예시)

공개키 암호화는 **공개키로 암호화**하고, **개인키로 복호화**합니다.

```python
from Crypto.PublicKey import RSA
from Crypto.Cipher import PKCS1_OAEP
import base64

# RSA 키 생성
key = RSA.generate(2048)
public_key = key.publickey()
cipher_rsa = PKCS1_OAEP.new(public_key)

message = "Hello Asymmetric World!"

# 암호화 (공개키 사용)
encrypted = cipher_rsa.encrypt(message.encode())
print("암호문:", base64.b64encode(encrypted).decode())

# 복호화 (개인키 사용)
cipher_rsa_priv = PKCS1_OAEP.new(key)
decrypted = cipher_rsa_priv.decrypt(encrypted).decode()
print("복호문:", decrypted)
```

**출력 예시:**
```
암호문: MIIBy...==   (난수처럼 보이는 문자열)
복호문: Hello Asymmetric World!
```

---

## 📊 비교 요약

| 방식 | 키 사용 | 속도 | 활용 |
|------|---------|------|------|
| **대칭키 (AES)** | 암호화·복호화 같은 키 | 빠름 | 대량 데이터 암호화 |
| **공개키 (RSA)** | 암호화·복호화 다른 키 | 느림 | 키 교환, 디지털 서명 |

---

👉 실제 인터넷 보안(예: HTTPS)에서는 **두 방식을 혼합**합니다. 메시지는 빠른 대칭키로 암호화하고, 그 대칭키를 안전하게 전달하기 위해 공개키 암호화를 사용하죠.  
