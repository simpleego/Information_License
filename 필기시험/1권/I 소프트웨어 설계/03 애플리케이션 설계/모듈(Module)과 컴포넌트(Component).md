# 모듈(Module)과 컴포넌트(Component)

소프트웨어 공학에서 **모듈(Module)**과 **컴포넌트(Component)**는 모두 시스템을 쪼개는 '단위'를 의미하지만, 바라보는 관점과 사용되는 단계에서 큰 차이가 있습니다.

쉽게 요약하자면, **모듈은 '설계와 구현'의 단위**이고, **컴포넌트는 '실행과 배포'의 단위**입니다.

---

## 1. 모듈 (Module): 구현의 관점

모듈은 프로그램을 구성하는 시스템의 일부분으로, 주로 코딩 단계에서 논리적으로 분리된 소스 코드 파일을 의미합니다.

* **관점:** 코드 작성자(개발자)의 시선
* **특징:**
* 특정 기능을 수행하는 코드의 집합입니다.
* `import`, `include` 등을 통해 소스 코드 수준에서 재사용됩니다.
* 주로 정적(Static)인 상태, 즉 컴파일되기 전의 코드 덩어리를 뜻할 때가 많습니다.


* **예시:** 파이썬의 `.py` 파일, 자바의 클래스(`class`)나 패키지, C언어의 `.c`와 `.h` 파일 등.

---

## 2. 컴포넌트 (Component): 실행의 관점

컴포넌트는 시스템 안에서 독립적으로 교체되거나 조립될 수 있는 실행 가능한 단위입니다. 하드웨어의 부품처럼 인터페이스를 통해 서로 연결됩니다.

* **관점:** 시스템 구조(아키텍처)와 사용자(운영자)의 시선
* **특징:**
* 독립적인 기능을 수행하며, 명확한 인터페이스를 통해 다른 부분과 소통합니다.
* 런타임(Runtime), 즉 프로그램이 실행되는 중에 의미를 갖는 단위입니다.
* 내부 구현을 몰라도 '블록'처럼 갈아 끼울 수 있는 **독립성**이 핵심입니다.


* **예시:** UI 라이브러리의 버튼 컴포넌트, 데이터베이스 접속 컴포넌트, 독립적으로 배포되는 DLL 또는 JAR 파일.

---

## 3. 핵심 차이점 비교

| 구분 | 모듈 (Module) | 컴포넌트 (Component) |
| --- | --- | --- |
| **주요 초점** | 코드의 논리적 분할 (Structure) | 독립적 배포 및 조립 (Deployment) |
| **결합 형태** | 소스 코드 수준의 결합 | 인터페이스를 통한 결합 |
| **성격** | 정적 (Static) - 설계/구현 시점 | 동적 (Dynamic) - 배포/실행 시점 |
| **재사용 단위** | 함수, 클래스, 소스 파일 | 실행 파일, 라이브러리, 서비스 |
| **비유** | 레고 블록을 만들기 위한 **재료** | 이미 만들어져 조립만 하면 되는 **레고 조각** |

---

## 4. 관계 이해하기

보통 **하나의 컴포넌트는 여러 개의 모듈로 구성**됩니다. 예를 들어, '로그인 컴포넌트' 안에는 '비밀번호 암호화 모듈', 'DB 조회 모듈', '사용자 인증 모듈'이 포함되어 있을 수 있습니다.

---

### 💡 요약

* **모듈**은 "코드를 어떻게 잘 나누어 작성할까?"에 대한 답입니다. (내부 구조 중심)
* **컴포넌트**은 "이 부품을 어떻게 조립해서 시스템을 만들까?"에 대한 답입니다. (외부 인터페이스 중심)
